services:
  app:
    image: php:8.2-fpm
    build:
      dockerfile: Dockerfile
    ports:
      - 8000:8000
    volumes:
      - '.:/var/www/html'
    networks:
      - app_network
    environment:
      - APP_ENV=production

networks:
  app_network:
    driver: bridge
